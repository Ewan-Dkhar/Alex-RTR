# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0    # The ASGI server to run FastAPI
python-dotenv>=1.0.1         # Environment variable support (.env)

# Data Validation & Settings
pydantic>=2.6.0
pydantic-settings>=2.2.0     # For parsing the .env file

# Database
sqlalchemy>=2.0.29           # ORM (used by existing code)
alembic>=1.13.1              # Database migration tool

# Security
passlib[bcrypt]>=1.7.4       # Password hashing
python-jose[cryptography]>=3.3.0  # JWT token generation
python-multipart>=0.0.9      # Needed if you accept form data or file uploads

# Testing
pytest>=8.1.1
httpx>=0.27.0                # Used by FastAPI's TestClient AND web tool (Serper/Firecrawl)

# Data / Vector
pandas>=2.2.0
faiss-cpu>=1.13.0            # In-memory FAISS vector store (Engine 1)

# LangChain + LangGraph (multi-agent orchestration)
langchain-core>=0.3.0
langchain-groq>=0.3.0              # ChatGroq — Groq API (primary LLM backend)
langchain-openai>=0.3.0            # Kept for compatibility (no longer primary)
langchain-community>=0.4.0         # OllamaEmbeddings (nomic-embed-text)
langchain-ollama>=1.1.0            # OllamaEmbeddings backend
langgraph>=0.2.0             # Multi-agent graph orchestration

# LangGraph SQLite Checkpointing (persistent state & follow-up threads)
langgraph-checkpoint-sqlite>=3.0.0

# Web Tools (Engine 3 — Serper, Engine 4 — Firecrawl)
# httpx is already listed above (dual-purpose: testing + web tool HTTP calls)

# Utilities
tenacity>=9.0.0
